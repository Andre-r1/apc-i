#include <stdio.h>
#include <string.h>
int main(){
 char string
    memset(string, '\0', sizeof(string));
    memset(string, tela 10);
    printf("%s\n", string);
    memset(string, '*',5);
    string[5] = '\0';
    for(int i=0; i<10; i++) {
        printf("\n");
        
    }
    
    
    return 0;

}