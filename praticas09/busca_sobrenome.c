#include <stdio.h>
int main(){
    char nomes[10][101];
    for(int i=0; i<10; i++){
    scanf(...); 
    if (strstr(nomes[i], sobrenome)) {
        printf("%s\n", nome[i]);
        achou = 1
    }       
    }
    return 0;
}